apiVersion: v1
kind: ConfigMap
metadata:
  name: leave-system-config
  namespace: leave-management
  labels:
    app: leave-system
data:
  APP_NAME: "Leave Management System"
  APP_VERSION: "1.0"
  LOG_LEVEL: "INFO"
  ENVIRONMENT: "production"
  JAVA_HEAP_SIZE: "256m"